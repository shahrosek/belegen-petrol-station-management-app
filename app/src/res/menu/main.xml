<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_add_emp"
        android:icon="@drawable/ic_emp_add"
        app:showAsAction="ifRoom"
        android:visible="false"/>
    <item android:id="@+id/action_add_cont"
        android:icon="@drawable/ic_cont_add"
        app:showAsAction="ifRoom"
        android:visible="false"/>
    <item android:id="@+id/action_upd_emp"
        android:icon="@drawable/ic_btn_update"
        app:showAsAction="ifRoom"
        android:visible="false"/>
</menu>
